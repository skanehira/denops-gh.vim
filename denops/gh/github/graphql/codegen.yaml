schema: schema.docs.graphql
generates:
  operations.ts:
    documents:
      - ../issue.ts
    plugins:
      - typescript-operations
    config:
      skipTypename: true
    preset: import-types
    presetConfig:
      typesPath: ./types.ts
  types.ts:
    plugins:
      - typescript
